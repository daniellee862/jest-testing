const checkUsernames = require("../katas/check-usernames");

describe("checkUsernames", () => {
  test("returns false when passed empty array", () => {
    expect(sumDigits(1)).toBe(1);
  });

  //   test("sumDigits returns the sum when passed multi digit number", () => {
  //     expect(sumDigits(99)).toBe(18);
  //     expect(sumDigits(123)).toBe(6);
  //   });

  //   test("ignore non-digit character", () => {
  //     expect(sumDigits(10.5)).toBe(6);
  //     expect(sumDigits(21.7)).toBe(10);
  //   });
});
